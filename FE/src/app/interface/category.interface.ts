// src/app/interface/category.interface.ts

export interface ICategory {
    categoryId: number;         // ID danh mục
    categoryName: string;       // Tên danh mục
    status: boolean;            // Trạng thái (true: hiển thị, false: ẩn)
  }
  